/*const { Model } = require('sequelize');

module.exports = (sequelize, DataTypes) => {
  class ConfigTest extends Model {
    static associate(modeld) {

    }
  }

  ConfigTest.init({

  })

  return ConfigTest;
}*/